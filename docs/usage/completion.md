---
title: "Shell Completion"
nav_order: 1
layout: default
parent: 🧭 Usage

---

To enable shell completion for xget, run the following command for your shell:

```sh
xget completion [SHELL]
```

```text
Generate the autocompletion script for xget for the specified shell.
See each sub-command's help for details on how to use the generated script.

Usage:
  xget completion [command]

Available Commands:
  bash        Generate the autocompletion script for bash
  fish        Generate the autocompletion script for fish
  powershell  Generate the autocompletion script for powershell
  zsh         Generate the autocompletion script for zsh

Flags:
  -h, --help   help for completion

Use "xget completion [command] --help" for more information about a command.
```
